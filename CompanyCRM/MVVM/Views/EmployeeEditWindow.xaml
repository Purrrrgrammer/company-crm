<Window x:Class="CompanyCRM.MVVM.Views.EmployeeEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сотрудник" Height="300" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ФИО" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding FullName}" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="2" Text="Должность" FontWeight="Bold" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="3" 
                  ItemsSource="{Binding Positions}"
                  DisplayMemberPath="Value"
                  SelectedValuePath="Key"
                  SelectedValue="{Binding Position}"
                  Margin="0,0,0,15"/>

        <TextBlock Grid.Row="4" Text="Дата рождения" FontWeight="Bold" Margin="0,0,0,5"/>
        <DatePicker Grid.Row="4" 
                    SelectedDate="{Binding BirthDate}"
                    Margin="0,25,0,15"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Width="100" Margin="0,0,10,0"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="100"/>
        </StackPanel>
    </Grid>
</Window>